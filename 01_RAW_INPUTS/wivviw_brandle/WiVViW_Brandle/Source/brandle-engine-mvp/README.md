# Brandle Engine MVP (Voice-first)

## Run
```bash
npm install
npm run dev
```

Open http://localhost:3000

Notes:
- Speech-to-text uses browser SpeechRecognition (Chrome recommended).
- TTS uses SpeechSynthesis (MVP).
- Domain/handle/trademark are **signals** with confidence, not guarantees.
